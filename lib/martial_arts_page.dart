import 'package:flutter/material.dart';

class MartialArtsPage extends StatelessWidget {
  const MartialArtsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Martial Arts")),
      body: Center(child: Text("Martial Arts Content Coming Soon")),
    );
  }
}
